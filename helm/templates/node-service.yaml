apiVersion: v1
kind: Service
metadata:
  name: nodeapp-service

  
spec:
  selector:
    app: nodeapp
  
  type: ClusterIP
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 8080
    protocol: TCP